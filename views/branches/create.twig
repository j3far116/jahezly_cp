{% extends "layout.twig" %}
{% block title %}إضافة فرع{% endblock %}

{% block content %}
<div class="d-flex justify-content-between align-items-center mb-3">
  <div>
    <h1 class="h4 mb-1">إضافة فرع</h1>
    <div class="text-muted">المتجر: <strong>{{ market.name }}</strong></div>
  </div>
  <a href="{{ markets_base }}/{{ market.id }}" class="btn btn-secondary">رجوع للمتجر</a>
</div>

<form method="post" action="{{ branches_base }}">
  {% include "branches/_form.twig" %}
  <div class="mt-3"><button class="btn btn-primary" type="submit">حفظ</button></div>
</form>
{% endblock %}
