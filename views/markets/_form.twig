<div class="mb-3">
  <label class="form-label">اسم المتجر</label>
  <input type="text" name="name" class="form-control {% if errors.name %}is-invalid{% endif %}" value="{{ values.name }}" maxlength="100" required>
  {% if errors.name %}<div class="invalid-feedback">{{ errors.name }}</div>{% endif %}
</div>

<div class="mb-3">
  <label class="form-label">الوصف</label>
  <textarea name="desc" class="form-control" rows="4">{{ values.desc }}</textarea>
</div>

<div class="mb-3">
  <label class="form-label">نوع المتجر</label>
  <select name="type" class="form-select">
    <option value="1" {% if values.type == 1 %}selected{% endif %}>المطاعم</option>
    <option value="2" {% if values.type == 2 %}selected{% endif %}>الكافيهات</option>
    <option value="3" {% if values.type == 3 %}selected{% endif %}>سوبر ماركت</option>
  </select>
</div>


<div class="mb-3 mt-3">
  <label class="form-label">الحالة</label>
  <select name="status" class="form-select {% if errors.status %}is-invalid{% endif %}">
    <option value="active"   {% if values.status == 'active' %}selected{% endif %}>نشط</option>
    <option value="inactive" {% if values.status == 'inactive' %}selected{% endif %}>غير نشط</option>
  </select>
  {% if errors.status %}<div class="invalid-feedback">{{ errors.status }}</div>{% endif %}
</div>

<input type="hidden" name="_csrf" value="{{ _csrf }}">
