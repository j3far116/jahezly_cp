{% extends "layout.twig" %}
{% block title %}إضافة وحدة جديدة{% endblock %}

{% block content %}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h1 class="h4 mb-0">إضافة وحدة جديدة</h1>
    <a href="{{ base }}" class="btn btn-light">رجوع</a>
</div>

{% if errs %}
    <div class="alert alert-danger">
        {% for e in errs %}
            <div>{{ e }}</div>
        {% endfor %}
    </div>
{% endif %}

<form method="post" action="{{ base }}">
    {% include "grocery_units/_form.twig" with { old: old } %}
    <div class="mt-3">
        <button type="submit" class="btn btn-primary">حفظ</button>
    </div>
</form>

{% endblock %}
