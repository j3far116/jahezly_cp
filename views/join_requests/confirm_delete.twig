{% extends "layout.twig" %}
{% block title %}تأكيد الحذف{% endblock %}

{% block content %}

<h1 class="h5 mb-3">تأكيد حذف طلب الانضمام</h1>

<div class="card">
  <div class="card-body">

    <p class="mb-3">
      هل أنت متأكد من حذف طلب المتجر:
      <strong>{{ req.store_name }}</strong>؟
    </p>

    <div class="d-flex justify-content-end gap-2">
      <a href="/admincp/join-requests" class="btn btn-light">إلغاء</a>

      <form method="post" action="/admincp/join-requests/{{ req.id }}/delete">
        <input type="hidden" name="_csrf" value="{{ _csrf }}">
        <button class="btn btn-danger">تأكيد الحذف</button>
      </form>
    </div>

  </div>
</div>

{% endblock %}
